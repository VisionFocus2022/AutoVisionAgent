## WRITER E:\学习项目\视觉大模型\tests\test_gui_predict_tail.py
- tests=19 passed=True status=green
- notes: 覆盖验证：两 predict 测试文件合并跑 --cov=gui/pages/predict/page.py → 97%（478 语句 / 12 未覆盖），唯一缺口 559-577 是 _export_csv，不在任务缺口清单内，由 tests/test_gui_pages_more.py 覆盖（已核实该文件存在两处 _export_csv 测试），全量套件下该文件应近 100%。任务清单所有目标行已覆盖：55/58（_boxes_to_jsonable 直调，None 与纯 list/tuple 无 tolist 兜底 + json.dumps 可序列化断言）、222（pick_open_file 取消）、232-235（旧引擎 unload 抛 RuntimeError/AttributeError 双吞，task 提示行 222 实为此分支已一并覆盖）、246-254（设备解析四分支：cuda 可用保持 cuda / torch.cuda 不可用回退 cpu / torch ImportError 回退 / core.config ImportError 保持 cpu——本机 torch+CUDA 真可用，用 monkeypatch torch.cuda.is_available 与 sys.modules None 灯控各分支，按序排列避免 monkeypatch 冲突）、

## WRITER tests/test_labeling_controller_deep.py + tests/test_data_manager_ext_deep.py
- tests=37 passed=True status=green
- notes: 无真 bug 发现：两文件均无 numpy 真值、except 元组漏收、QPixmap 越线程三家族问题。新文件单跑 controller 94% / ext 96%；与既有 tests/test_labeling.py、test_m1_e2e.py 合跑两文件均 100%（含全部指定缺失行）。关键技术点（已在写测试前用一次性脚本实证）：① QGraphicsView 默认 640x480 且场景小于视口时 AlignCenter 居中偏移、初始滚动条居中，故用 set_blank(300,300)+scale(10,10)+滚动条归零得到精确 mapToScene(x,y)==(x/10,y/10)，断言与原始 view 坐标明显不同，证明真实坐标转换；② QMouseEvent.pos() 是整型 QPoint，mapToScene 不收 QPointF，鸭子事件按 QPoint 构造；③ fallback 分支（31-47）用 monkeypatch.setitem(sys.modules, "project.models", None) + importlib 以新名 exec 模块源码触发（Python 对 sys.modules 值为 None 的模块抛 ImportError），不污染全局缓存；④ handle_commit 的 while 清空分支（182-186

## WRITER E:\学习项目\视觉大模型\tests\test_exporter_deep.py
- tests=21 passed=True status=green
- prod: supervised_exporter.py :: 第 53-54 行：自动 input_shape 分支的 task 读取由 getattr(engine.task, "value", "") 改为 getattr(getattr(engine, "task", None), "value", "")——修复无 task 属性引擎在 export_supervised_engine 防御路径（unknown 命名分支）下必现的 AttributeError 裸穿；对有 task 的合规引擎行为不变。
- BUG supervised_exporter.py:54 :: export_onnx 对无 task 属性的引擎崩溃：getattr(engine.task, "value", "") 在 engine.task 本身缺失时直接抛 AttributeError（getattr 默认值只作用于 value 层）。而同文件 export_supervised_engine:284 用 hasattr(engine, "task") 显式支持无 task 引擎（命名 "unknown_model.onnx"），其防御分支一旦走入必崩（便捷函数从不传 input_shape，必走自动选形路径）。最小修复：task_val = getattr(getattr(engine, "task", None), "value", "")，对有 task 的合规引擎行为完
- notes: 覆盖率 12%→73%（106/145 语句），未达 80% 目标：剩余 39 行全部为依赖缺失门控，按任务纪律不伪造。明细：141-182（TRT 真转换全路径，tensorrt 未装，仅覆盖 135-139 显式跳过降级）28 行；202-206（onnxsim 简化成功路径，onnxsim 未装）5 行；215-220（onnxconverter_common fp16 真转换，未装）6 行；193（onnx ImportError 分支，本机 onnx 已装）。onnxruntime 已装故 int8 量化为真路径：静态量化真跑成功（quant_format=None 实测可用，产出 .int8.onnx），回退动态量化用故障注入 monkeypatch onnxruntime.quantization.quantize_static 抛错触发（被测的 except Exception→quantize_dynamic 回退逻辑真实执行，动态量化真跑落盘）。真路径亮点：torch.onnx.export 真导出（含动态轴断言 dim_param=\"batch_size\"）、onnx.checker 真校验、6 任务自动 input_shape 全分支（torch.randn spy 透传记录形状）。回归：test_exporter_and_utils.py(13) 与 

## WRITER E:\学习项目\视觉大模型\tests\test_engines_family_deep.py
- tests=51 passed=True status=green
- prod: pose_yolo.py :: 修复 labels 误按 int(confidence)（恒 0）索引的 bug：改为提取 r.boxes.cls 按类别 id 映射，并加 `int(k) < len(labels)` 越界守卫回退 person_i（与 det_yolo/_yolo_seg_base/pseg_yolo 对齐）
- prod: cls_torchvision.py :: infer 与 infer_batch 的 labels[int(pred)] 加越界守卫：标签表短于类别数时回退 f"class_{pred_i}"，不再抛裸 IndexError（与 det_yolo 对齐）
- BUG models/supervised/engines/pose_yolo.py:57-60 :: labels 参数按 int(confidence) 而非类别 id 索引：genexp `labels[int(c)] ... for i, c in enumerate(conf)` 中 conf 是置信度浮点数组，int(conf∈(0,1)) 恒为 0 → 只要传 labels，所有实例永远映射到 labels[0]（类别 id 完全被忽略）；且缺短标签表越界守卫（对照 det_yolo/seg/pseg 的 `int(c) < len(labels)`）。修复：提取 r.boxes.cls 并按类别 id 映射 + person_i 越界回退，与兄弟引擎对齐。
- BUG models/supervised/engines/cls_torchvision.py:64 与 91 :: infer/infer_batch 对 labels[int(pred)] 无越界守卫：模型预测类别 id ≥ len(labels) 时抛裸 IndexError（用户提供的标签表短于模型类别数即触发；模型常为 1000 类 ImageNet 头）。修复：pred_i 越界时回退 f"class_{pred_i}"，与 det_yolo 守卫风格一致。
- notes: 新建 tests/test_engines_family_deep.py：51 个测试项（44 unit + 7 integration/含 6 参数化展开），全部通过（4.1s）。五文件覆盖率（与 tests/test_engine_m2_contracts.py 合并实测）：super_cv2 36%→100%、cls_torchvision 39%→100%、_yolo_seg_base 39%→100%、pose_yolo 57%→100%、pseg_yolo 52%→100%（219 stmts / 0 miss）。手段：① super_cv2 用记录型 DnnSuperResImpl_create 替身覆盖文件名推断 edsr/espcn/fsrcnn/lapsrn+scale 正则、显式覆盖、ascii_path_copy 中文路径（readModel 收到 ASCII 临时拷贝且内容一致）、setModel 小写 cv2.error→capitalize 回退、readModel 失败包装、GRAY→BGR、路径读图、_to_numpy 全分支；cv2 缺失分支用 monkeypatch.setitem(sys.modules,'cv2',None) 真触发 ImportError。② cls 用 monkeypatch _safe_torch_load（I/O 边

## VERIFIER E:\学习项目\视觉大模型\tests\test_exporter_deep.py
- exit=0 passed=True verdict=accept
- coverage: exporter/supervised_exporter.py 覆盖 12%（基线）→ 73%（145 语句、39 缺失：141-182、193、202-206、215-220）。剩余缺口全部为依赖门控分支，本环境不可达：141-182 需真 tensorrt（未装，测试显式 skip 不伪造）、193 需 onnx 未安装（已装）、202-206 需 onnxsim（未装，走了 ImportError 跳过分支已覆盖）、215-220 需 onnxconverter_common（未装，fp16 ImportError 分支已覆盖）。基线缺口 50-111/186-195/198-208/212-262/283-299 已全部填平（真 torch.onnx.export IO、真 onnx 校验、真 int8 静态/动态量化、真便捷函数路径）。注意：--cov 传文件路径无效（coverage 报 module-not-imported），必须用模块名 --cov=exporter.supervised_exporter。
- fake_greens: []
- out_of_scope: ["models/supervised/engines/cls_torchvision.py（modified，非本簇产物：任务列表 #51 W10-T3 引擎家族 in_progress，并行编写代理正", "models/supervised/engines/pose_yolo.py（同上，W10-T3 并行簇产物）", "tests/test_engines_family_deep.py（untracked，W10-T3 簇测试文件）", "tests/test_data_manager_ext_deep.py、tests/test_labeling_controller_deep.py（untracked，W10-T4 簇产物）", "tests/test_gui_core_tail.py、tests/test_gui_predict_tail.py（untracked，W10-T5 簇产物）", ".workflow/wave10-deep-swamps/、docs/prd-wave10-deep-swamps.md、docs/tasks-wave10-deep-swamps
- bug confirmed: supervised_exporter.py:54 | RED 证据经机械复现确证：git stash 还原修复后运行 tests/test_exporter_deep.py::TestExportSupervisedEngine::test_taskless_engine_exports_as_unknown，在 exporter/supervised_exporter.py:54 'task_val = getattr(engine.task, "value", "")' 处抛 AttributeError: '_TasklessEngine' object has no attribute 'task'，与自报证据逐字一致。逻辑链核实成立：e

## WRITER E:\\学习项目\\视觉大模型\\tests\\test_gui_core_tail.py
- tests=23 passed=True status=green
- notes: 23 unit tests, all green. Coverage (this file alone): shell.py 100%, tasks_ui.py 100%, theme.py 95% (192-194 = resolve_theme Light/Dark success branches, covered by existing test_theme_auto.py), thread_bridge.py 88% (38, 69 = Q_ARG return + multi-arg invokeMethod, covered by existing test_threaded_pages.py). Combined run with those two sibling files: all four gui/core modules at 100% (290/290 stmts). Target missing lines all hit: shell 178/199/208-212/216-224/230-232/236-238/253-254/260-261/264-273/279-280; tasks_ui 37-38/62/69/72/75 (via fake registry injected by monkeypatching models.supervi

## VERIFIER E:\学习项目\视觉大模型\tests\test_labeling_controller_deep.py + E:\学习项目\视觉大模型\tests\test_data_manager_ext_deep.py (17+20=37 tests)
- exit=0 passed=True verdict=accept
- coverage: Deep-only run (--cov=labeling.controller --cov=industrial_vision_platform.data_manager_ext, 37 tests): labeling/controller.py 62%→94% (Stmts 128, Miss 8; residual missing 154-158,164-166 = handle_move/handle_release 便捷 API 带 labeler 的主体，基线未列入缺失清单，即现有套件已覆盖，并集≈100%；基线缺失行 65,73-75,95-96,102-114,117-125,128-135,143,153,163,176,182-186,190-192,205 全部落到——含非法模式降级、真实 Qt mapToScene 缩放+滚动条坐标转换、三鼠标事件全链路矩形闭环、右键提交多边形、AUTO 队列 if+while 双分支、cancel、attach_interactive 三态)。industrial_vision_platform/data_manager_e
- fake_greens: [{"test": "tests/test_labeling_controller_deep.py::test_attach_interactive_rejects_non_interactive_mode (lines 316-319)", "why": "轻微语义重复：与现有 tests/test_sam_wiring.py:121-128（attach_interactive 非交互式模式返回 False）测的是同一分支（controller.py:202-203），该分支基线已覆盖，非新增覆盖点。非严格假绿：断言可失败、走真实生产代码、无误导性；可选删除或保留作三态契约文档。其余 36 条未发现假绿：无不可能失败断言（早退守卫若被删除会以 AttributeError 失败）、无整体 monkeypatch 被测生产代码（_ScriptedLabeler 只桩 ILabeler 协
- out_of_scope: ["exporter/supervised_exporter.py (tracked file modified, +2/-2: engine.task getattr 防穿) — 属簇外生产模块改动，但", "tests/test_exporter_deep.py (untracked) — 疑似 W10-T2 交付物", "tests/test_engines_family_deep.py (untracked) — 疑似 W10-T3 交付物", "tests/test_gui_core_tail.py, tests/test_gui_predict_tail.py (untracked) — 疑似 W10-T5（进行中）交付物", ".workflow/wave10-deep-swamps/, docs/prd-wave10-deep-swamps.md, docs/tasks-wave10-deep-swamps.md (unt"]

## VERIFIER E:\学习项目\视觉大模型\tests\test_engines_family_deep.py
- exit=0 passed=True verdict=needs_fix
- coverage: 本文件单独跑（PY -m pytest -o addopts="--tb=short" --cov=models/supervised/engines --cov-fail-under=0 -q tests/test_engines_family_deep.py，51 passed）：super_cv2 36%→97%（缺 48,109——load 缺路径/infer 未加载 raise，全量门禁由 tests/test_engine_m2_contracts.py 覆盖）；cls_torchvision 39%→96%（缺 26,54，同为 m2 已覆盖的 raise 行）；_yolo_seg_base 39%→100%；pose_yolo 57%→94%（缺 25,41，m2 已覆盖）；pseg_yolo 52%→94%（缺 26,42，m2 已覆盖）；附带 seg_yolo 100%。基线缺口行（super_cv2 51-99,111-120,129,134,138-143；cls 30-37,55-65,78-97；_yolo_seg_base 29-37,46-68；pose
- fake_greens: [{"test": "tests/test_engines_family_deep.py::TestRealYoloInfer::test_seg_base_real_model_infer", "why": "靠未固定种子的随机权重碰运气：fixture 用 YOLO(\"yolov8n-seg.yaml\") 以进程默认 torch RNG 构建随机权重模型，实测探针显示 12 个 torch 种子中 seed=6 在 conf=0.0 下产出 0 框（其余 54~300 框），assert len(res.boxes) > 0 即失败。本验证员的覆盖率复跑已真实触发该失败（2 failed），复跑又 51 全绿——非确定性，自报「全绿」只是抽中了好种子。"}, {"test": "tests/test_engines_family_deep.py::TestRealYoloInfer
- out_of_scope: ["exporter/supervised_exporter.py 被修改（getattr(engine, \"task\", None) 防穿守卫）——不在本簇声称的生产改动清单内，也不在 engines ", "tests/test_exporter_deep.py（未跟踪）——W10-T2 簇产物", "tests/test_labeling_controller_deep.py、tests/test_data_manager_ext_deep.py（未跟踪）——W10-T4 簇产物", "tests/test_gui_core_tail.py、tests/test_gui_predict_tail.py（未跟踪）——W10-T5 簇产物（任务表显示 pending）", ".workflow/wave10-deep-swamps/、docs/prd-wave10-deep-swamps.md、docs/tasks-wave10-deep-swamps.md（未跟踪）——", "注：本簇测试文件不依赖 exporter 改动（无 exporter 导入）；无 pytest.ini
- bug confirmed: models/supervised/engines/pose_yolo.py:57-64 (修复前行 55-60) | 经 git checkout 回滚修复后实测复现 RED：cls=[5,7]、conf=[0.9,0.8]、labels=p0..p9 时旧行为返回 ('p0','p0') 而非 ('p5','p7')——旧代码 `labels[int(c)] for i, c in enumerate(conf)` 以置信度取整索引（conf∈(0,1) 恒 int=0），类别 id 完全被忽略，与自报证据逐字吻合；短标签表用例实测 ('only',) != ('person_0',) 亦吻合。旧代码确无 `int(c) < len(labels)` 越界守卫（对照 _yolo_seg_base.py:73
- bug confirmed: models/supervised/engines/cls_torchvision.py:64 与 91（修复前行号） | 回滚修复后实测：bias 固定微型网恒预测 class 3、labels=['a','b','c']（len 3）时 `IndexError: list index out of range` 精确落在 cls_torchvision.py:64 `label = labels[int(pred.item())]...`，infer_batch 同样精确落在 :91——与自报证据（含行号）逐字吻合；旧行 `if labels else` 分支只判非空不判长度，确无越界守卫。且 test_infer_labels_mapped / test_infer_batch_labels_mapped（l
- FIX: 只需修 flaky，其余交付质量良好（覆盖达标、两 bug 修复经回滚复验为真、无 m2 语义重复、替身只注入外部 I/O 边界未遮挡被测生产代码）。具体改 tests/test_engines_family_deep.py 两个 module 级 fixture：1) real_seg_yolo（约行 790-795）：在 `from ultralytics import YOLO` 之后、`return YOLO(\"yolov8n-seg.yaml\")` 之前加 `torch.manual_seed(0)`（seed 0 已实测：conf=0.0 得 300 框；seed 6 为已证伪的 0 框坏种子）；2) real_pose_yolo（约行 798-809）：在 `YOLO(\"yolov8n-pose.yaml\", task=\"pose\")` 之前同样加 `torch.manual_seed(0)`（seeds 0-4 已实测均 300 框、kpts (300,17,3)）。module 级 fixture 内在构建前设种子可使其不依赖先前测试对全局 RNG 的消耗，模型权重确定化；torch 模块顶部已导入。改后连跑该文件 ≥5 次验证全绿稳定（本验证员当前 5 次复跑中 1 次翻车，修复后应 0 次）。不要用放宽断言（len≥0）代替——那会掏空测试价值。

## VERIFIER E:\学习项目\视觉大模型\tests\test_gui_core_tail.py
- exit=0 passed=True verdict=accept
- coverage: 复跑 E:\学习项目\视觉大模型\tests\test_gui_core_tail.py：exit 0，23 passed，与自报一致（0.19s）。定向覆盖（--cov=gui.core.shell/tasks_ui/theme/thread_bridge）：仅本文件即 shell.py 83%→100%（基线缺 178,199,208-212,216-224,230-232,236-238,253-254,260-261,264-273,279-280 全部补齐）；tasks_ui.py 82%→100%（37-38,62,69,72,75 齐补）；theme.py 92%→95%（基线缺 195-196,221-223 补齐，余 192-194 由既有 tests/test_theme_auto.py 覆盖）；thread_bridge.py 基线缺的 34（TypeError 拒绝）已补，单文件 88% 是因 38/69（带参 Q_ARG 路径）由既有 tests/test_threaded_pages.py 覆盖。合并相关测试文件（test_gui/test_tasks_ui
- fake_greens: []
- out_of_scope: ["M exporter/supervised_exporter.py（疑为并行簇 W10-T2 exporter 产物）", "M models/supervised/engines/cls_torchvision.py（疑为并行簇 W10-T3 引擎家族产物）", "M models/supervised/engines/pose_yolo.py（疑为并行簇 W10-T3 引擎家族产物）", "?? tests/test_exporter_deep.py（并行簇产物）", "?? tests/test_engines_family_deep.py（并行簇产物）", "?? tests/test_labeling_controller_deep.py（并行簇产物）", "?? tests/test_data_manager_ext_deep.py（并行簇产物）", "?? tests/test_gui_predict_tail.py（W10-T5 同任务的 gui_predict 子簇产物，非 gui_core 簇）", "?? .workflow/wave10-deep-swamp

## VERIFIER E:\学习项目\视觉大模型\tests\test_gui_predict_tail.py
- exit=0 passed=True verdict=accept
- coverage: gui/pages/predict/page.py（簇内唯一生产模块，478 语句，git 未改动）：编写前基线（重建：test_gui_predict_flawgen + test_gui_pages_more + test_threaded_pages 三文件）= 90%，缺 48 行（55,58,222,246-247,252-254,265-267,279,309,333-334,352,389,397-407,496-509,566,582-583,628-629,634,688-689,694）；仅新文件单跑 = 88%（缺 55 行）；合并现有 predict 测试 + 新文件 = 99%（仅缺 566，即 _export_csv 取消路径，由套件其他文件覆盖）。任务给定的基线缺口清单逐行核对：全部由新文件覆盖（设备解析 246-254、逐张批量路径+取消 break 389/397-407、sv 回退 496-509、xlsx 654-683、导出空态/取消 628-629/634/688-689/694、_boxes_to_jsonable 55/58、单张取消/早退
- fake_greens: []
- out_of_scope: ["exporter/supervised_exporter.py（M）——属并行 W10-T2 exporter 簇的未提交改动，非 predict_tail 所为", "models/supervised/engines/cls_torchvision.py（M）——属并行 W10-T3 引擎簇", "models/supervised/engines/pose_yolo.py（M）——属并行 W10-T3 引擎簇", "tests/test_exporter_deep.py / test_engines_family_deep.py / test_data_manager_ext_deep.py / test_lab", "tests/test_gui_core_tail.py（??）——同波 T5 另一 gui 子簇交付文件", ".workflow/wave10-deep-swamps/ 与 docs/prd-wave10-deep-swamps.md、docs/tasks-wave10-deep-swamps.md（??）—"]

