# 变更卡: 环境清理与 UIA 真窗复跑（W19 exe）

| 字段 | 内容 |
|------|------|
| **日期** | 2026-08-18 |
| **档位** | L1（可逆 / 局部 / 立即或测试可见 / 低不确定） |
| **分档依据** | 纯运维验证任务：零生产代码、零测试断言改动；清理项（残留锁/陈旧 shm）均可再生或本就自愈；结果由测试套件直接给出 |

### What（改了什么）
- 环境核查与清理：残留 AutoVisionAgent 进程（无）、%TEMP% 单实例锁（发现 14:03 残留一枚，移除）、陈旧 ava_*.bin shm 区域（0 个）、磁盘/CPU/内存摸底。
- UIA 真窗复跑：`tests/uia` 全套 6 用例对 W19 重打包 exe（08-18 07:10）。

### Why（为什么改）
- W19 exe 当日三轮复跑 3F/2F/2F 且失败点漂移，已归因为机器级 COM/UIA 劣化（W16 同签名：应用侧完成而驱动层看不见+日志零异常）。用户指令：清理环境后复跑闭环。

### Files（涉及文件）
- 无生产/测试代码改动；仅移除 `%TEMP%\autovisionagent-single-instance.lock`（运行时自愈产物）。

### Verify（怎么验证）— 门禁三件套
- **命令**：`.venv/Scripts/python.exe -m pytest tests/uia -o addopts=`
- **预期**：6/6 全绿，退出码 0（历史健康基线 3:59）
- **不达标分支**：失败点若再现漂移签名（flaky 路由）允许按 W16 先例复跑至多 2 次；确定性失败则停止并如实上报，不改断言

### Rollback（如何回滚）
- 无代码改动无需回滚；锁文件与 shm 区域由应用运行时按需重建。

### 启动条件
- [x] `request_evidence` 已记录用户原始目标；本任务为纯验证/运维，无行为变化，RED 测试豁免（偏差留痕见 state.json deviations）
