# 异常检测（anomalib 口径；SKolpha 对应 01_s_abdet_normal_v1.0.yaml）
task: abdet
variant: normal
backbone: patchcore_resnet18
img_size: 256
batch_size: 8
lr: 0.001
augmentation:
  hflip: 0.5
  rotate_max: 10
  translate: 0.1
  split_ratio: 0.8
