# Tasks：矩形内分割松框悬崖优化（W54 · lite）

关联：[prd-sam3-region-cliff-fix.md](prd-sam3-region-cliff-fix.md)

| # | 任务 | 依赖 | 验证 | 状态 |
|---|------|------|------|------|
| T1 | 定档 + PRD/tasks 落盘（自治 S1+S3） | — | 本文档 | ✅ |
| T2 | `scripts/exp_sam3_region_prompt_sweep.py`：A1 补全 + A2-50/A2-75 + A5，margin {0,8,16,64} 全曲线，GPU 后台 | T1 | A2 两臂全曲线落档；**A5/A1 因机器重度争抢停滞 ~1h 被裁撤**（决策让位于端到端） | ✅（部分） |
| T3a | 自适应链 v1（fill≥0.85 检测）实现 + TDD（4 用例 RED→GREEN，37 passed） | T2 | 单测绿 | ✅ |
| T3b | 链 v1 端到端生产路径复测 `scripts/exp_sam3_region_chain_e2e.py` | T3a | **证伪**：0.756/0.490/0.319✗/0.137✗（触发率 2%/10%/1%——大结构掩码对盒填充典型 0.6-0.7，0.85 阈几乎不触发；下调又误伤紧框） | ✅（证伪） |
| T3c | 路由器诊断轮 `scripts/exp_sam3_region_router_diag.py`：逐图记录 direct+tight 双臂 fill/spill/bbox_in/IoU → JSON，离线搜路由 | T3b | ✅ 648 条（162×4）双臂零缺失，落盘 router_diag_w54.json（22:23 完成） |
| T4 | 证据门落产（AC-2 三线）：路由器达标 → 换检测器落产 + 复测；不达标 → 回滚链至 W53 形单发 + 留档申报 | T3c | ✅ **回滚分支**（三族搜索 1419 规则+CV 树+双臂择优全灭、oracle m=16 上限 0.590 仅超兜底 +0.044——详见收官节；回滚已执行并逐行等价证明；v4 面积比复核中） |
| T5 | UIA `test_sam3_labeling.py` 真窗复跑 + 主门禁 `pytest tests/ -q` | T4 | ✅ UIA 3/3（1 例 folder-open 时序 flaky 隔离复跑绿，归因机器重争抢）+ 主门禁 1216 passed/rc=0/覆盖 ≥92 |
| T6 | 收官：数字回填 + EXP/learning 沉淀 + 记忆更新 | T5 | ✅ 见「T3c-T6 收官」节 |

## 执行约束

- GPU 单任务独占（~2h 后台），与 UIA 错峰；显存预检 ≥5GB 空闲。
- A2 半边长下限 16px 硬护栏（W52 r=8 零实例教训）。
- 不自动 commit（铁律 7）。

## 基线锚（W53 实测，val 162）

| margin | 现产（用户盒直发+nearest） | 固定紧提示混合（m≥16 已测） |
|---|---|---|
| m=0 | **0.755** / 99% / 0 | 待测（A1） |
| m=8 | 0.486 / 40% / 0 | 待测（A1） |
| m=16 | 0.275 / 11% / 0 | 0.546 / 63% / 1 |
| m=64 | 0.136 / 10% / 1 | 0.546 / 63% / 1 |

## T2 扫描实测（val 162）

| 臂 | m=0 | m=8 | m=16 | m=64 |
|---|---|---|---|---|
| A2-50（f=0.5 缩放提示+∩用户盒） | 0.670 ✗ | 0.581 | **0.620** ✓ | 0.195 ✗ |
| A2-75（f=0.75） | 0.724 ✗(差0.016) | **0.647** | 0.464 ✓(贴线) | 0.126 ✗ |
| A5 最小含点 / A1 紧提示 m=0,8 | 机器争抢停滞 ~1h 被裁撤（决策让位于端到端） | | | |

**结构性结论：固定比例缩放无法跨 margin 映射回 GT 尺度（非单调：f=0.5 在 m=16 最佳 0.620、m=64 仅 0.195）——任何单 f 都倒门。** 最优组合=分 margin 路由（直发/f=0.75/f=0.5/±16），但 m 不可知 → 需可靠检测器。

## T3b 链 v1 端到端（生产路径，含证伪记录）

| margin | 链 v1（fill≥0.85） | 前向均 | 门 |
|---|---|---|---|
| m=0 | 0.756 / 99% / 0 | 1.02 | ≥0.74 ✓ |
| m=8 | 0.490 / 41% / 0 | 1.02 | — |
| m=16 | 0.319 / 18% / 0 | 1.10 | ≥0.45 ✗ |
| m=64 | 0.137 / 10% / 1 | 1.01 | ≥0.30 ✗ |

**证伪归因**：fill 检测器双盲——大结构抓取时掩码∩盒/盒面积典型 0.6-0.7（非「充满 slab」假设），0.85 阈几乎不触发；下调至 ~0.55 则紧框正确抓取（缺陷对自身 bbox 填充 0.5-0.8）被误伤反噬 0.755。单阈值 fill 无分离力 → 转诊断轮（fill/spill/bbox 包含度）+ 离线路由搜索。

## T3c-T6 收官（2026-08-30 深夜 · 用户交办接手会话执行）

### T4 裁决：三族路由搜索全灭 → 回滚分支

| 路由族 | 最优 | m=0（≥0.74） | m=16（≥0.45） | m=64（≥0.30） |
|---|---|---|---|---|
| 先验特征路由（1419 条网格） | `bbox_in<0.94` | 0.733 ✗ 差0.007 | 0.449 ✗ 差0.001 | 0.401 ✓ |
| 决策树 d=2/3/4（5 折 CV·OOF 口径） | 退化为近似恒紧 | 0.655-0.668 ✗ | 0.542-0.546 ✓ | 0.486-0.546 ✓ |
| 双臂择优（恒 2 前向，零 GT 特征对比选臂） | min spill | 0.687 ✗ | 0.513 ✓ | 0.491 ✓ |

**釜底抽薪的上限论证**：oracle（逐记录 max(直发,±16)）= 0.788/0.632/0.590/0.574——m=16 头寸仅比「恒紧兜底 0.546」高 **+0.044**（162 样本 SEM≈0.024，≈2σ）。PRD 主目标「松框**显著**超 0.546 兜底」对**任意完美路由**结构性不可达——路由问题不是规则找不到，是解本身不够大。

**回滚执行**：链 v1（fill≥0.85 三级收紧，e2e 已证伪）+ `_fill_ratio`/`_tighter_prompt` 助手 + 4 个链测试移除；`predict_point_in_box` 还原 W53 形单发——与 0fa797f 已提交版**逐行逻辑等价**（diff 证明：仅类型标注现代化〔future-annotations 下零运行时影响〕+ docstring 加证伪留档指针）。守卫回归：test_sam3_adapter 33 passed。

### v4 复核（f=0.5 面积比二级参照 · 原会话执行完毕 · 终裁：全灭）

原会话补的第三臂设计：疑松判据 `area(f05)/area(direct) ≤ θ`（同物比值≈1，直发抓大结构则 f05 抓真目标 ≪1）——正攻 m=0 误路由尾部重叠盲区。f05 臂全量采集（`scripts/exp_sam3_region_f05_arm.py` → `weights/sam3-pole-ft/f05_arm_w54.json`，648 条零缺失）后离线精算：

**§3c 裁决（2026-08-30 深夜，原会话）**：
- §3c 网格 48 条中 13 条标 PASS **全为 `→best` 目标 = 逐记录 max(三臂 IoU) = 纯 oracle**（需 GT 才能选 max），不可部署——诚实目标是单兜底（→f05 / →tight）与两级兜底。
- **三臂 oracle 上界**：0.788 / 0.634 / **0.636** / 0.598——比两臂 oracle 的 m=16 头寸扩至 +0.090，但仅诚实规则可吃到才算数。
- **诚实（运行时可观测）规则全灭**（24 变体）：单兜底 ratio≤0.35-0.5→f05/→tight、两级 `ratio≤θ1 & f05.fill_prompt<θ2→f05|→tight`、最小面积变体——规律高度一致：**凡救起 m=16（0.51-0.61✓）与 m=64（0.41-0.55✓）的路由，m=0 必落 0.67-0.72（✗ 门 0.74）**。最优诚实线 ratio≤0.5→tight = 0.688/0.487/0.539/0.520。
- 归因与前三族同源：area 比值信号在 m=0 的 7-15% 尾部（f05/tight 恰好更差的图）无法与真松框案例分离——直发臂可观测几何不编码「盒松紧」。

**终裁：回滚终局成立（v4 不翻案）。** 四族路由（先验网格 1419 + CV 决策树 + 双臂择优 + 三臂 v4）+ 双 oracle 界（两臂/三臂）交叉验证同一结论：盒提示悬崖是 SAM3 盒语义的本质属性而非可路由缺陷；生产定版 = W53 形单发（`predict_point_in_box` 紧框 0.755，操作口径：拖紧框）。

### T5 验证

- UIA 真窗（`AVA_UIA_SOURCE=python`）：test_sam3_labeling **3/3**——首跑 `test_sam3_auto_concept_flow` 一次 folder-open 等待超时（flaky 类），隔离复跑 81s 绿；归因机器重争抢（SolidWorks MCP/aicad 全栈 + f05 采集同卡，本轮 201s vs W53 164s）。断言位置 `_open_label_folder` 开图基建，与 predict_point_in_box 零交集
- 主门禁：**1216 passed** / 5 skipped / rc=0 / 覆盖 ≥92（1220 − 4 链测试，数目严丝合缝）
