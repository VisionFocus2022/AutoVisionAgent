# AutoVisionAgent v6 深度审查（W35+W36 增量主审 + 全仓横切） — 精简 PRD (L2)

> 版本: 1.0 | 日期: 2026-08-23 | 档位: 🟡 L2 | 确定性: 高（探索门禁 3/3 裁决：增量主审+全仓横切 / 五视角并行 agent / 报告+分级只读） | 影响半径: 小（纯只读分析，不改生产代码；命中硬触发器：无） | 可逆性: 双向门（产出=评审文档）
> 前置：v5 报告（docs/AutoVisionAgent-架构解析与优化方案-v5.md，`ea1013b`）已覆盖 W26–W34；本审不重复其内容，只核销其宣称、深审其后增量。

## 1. 背景与目标

- **背景**：v2.1.0 已发版（`550ecbf`）。v5 架构复审止于 `ea1013b`，其后 W35（发版清偿：action 门控消费 / i18n 补译守卫 / lite 剪除）与 W36（imagePath 相对化 / RELEASES）两波增量未被任何审查覆盖；v5 报告宣称「四条 P2 全部清偿或根治」（见 RELEASES.md），该宣称本身未经验证。用户要求一轮比 v5 更深入全面的审查。
- **目标**：
  1. W35+W36 全部增量逐文件深审 + 全仓五视角横切，产出 v6 评审报告（P0-P3 分级，只读）
  2. v5 四条 P2 清偿宣称逐条核销（闭环 / 部分 / 证伪），延续「回归轮」体例
  3. 每条发现可直接开清偿波（含定位 / 证据 / 建议 / 建议波次）

## 2. 功能需求 (FR)

- **FR-001**: 增量主审 — `ea1013b..HEAD` 全部生产/脚本/测试变更逐文件深审：`core/session.py`、`gui/core/i18n.py`、`gui/core/permissions.py`、`gui/main.py`、`gui/pages/{label,predict,settings}` 页面、`gui/pages/predict/video_super_actions.py`（W35 增量）、`gui/pages/label/batch_prelabel.py`、`scripts/make_lite_dist.py`、`tests/`（W35 双测、UIA 取证、W36 测试），对照 prd-w35-release-hardening.md 的 FR/AC 与 W36 收尾宣称。 | P0
- **FR-002**: 全仓横切五视角 — 并行 agent：①事实核查 ②资深工程（可维护性/设计缺陷）③安全专家（密钥/注入/路径穿越/权限门控绕过）④一致性（命名/分层/i18n/页面规模守卫）⑤冗余（死代码/重复逻辑）；主线用 codegraph 追关键调用链（action 门控注册→消费、i18n 回退、session 生命周期、batch_prelabel imagePath 流）。 | P0
- **FR-003**: v5 遗留核销 — v5 §4 四条 P2 的清偿宣称逐条验证 + v5 覆盖矩阵状态复核（18 视角在本 HEAD 是否仍成立）。 | P1
- **FR-004**: 报告产出 — `docs/AutoVisionAgent-架构解析与优化方案-v6.md`，继承 v5 体例：总评 / v5 核销表 / 增量深审 / 缺点清单（P0-P3×五元组：定位 file:line、证据、影响、修复建议、建议波次）/ 实测指标 / 攻方复核台账 / 改进路线 / 覆盖矩阵 / 局限性。 | P0
- **FR-005**: 只读边界 — 不改任何生产/测试/配置代码；本审落盘仅限：v6 报告 + evolve/ 沉淀 + 本 PRD/任务文档。 | P0
- **FR-006**: 证据实测 — 报告中一切可执行断言（pytest 通过数、grep 计数、文件规模）来自本 HEAD 实测并附命令，不引用旧波次数值。 | P0

## 3. 验收标准 (AC)

- **AC-001**: 增量映射完备 — `git diff --name-only ea1013b..HEAD` 每个非 `.workflow`/`docs` 文件在 v6 报告有审查条目（含映射表）；W34 已被 v5 覆盖部分以引用标注而非重审。 [FR-001]
- **AC-002**: 五视角各有产出 — 5 个 agent 各自返回经验证发现或显式「该维度无新发现」；P0/P1 级发现全部经事实核查视角复核（file:line 引用与实际代码一致）。 [FR-002]
- **AC-003**: v5 核销闭环 — 四条 P2 宣称各有独立结论（闭环/部分闭环/证伪），附证据命令。 [FR-003]
- **AC-004**: 发现可执行 — 缺点清单每条含五元组全字段；缺定位或缺证据条目数 = 0。 [FR-004]
- **AC-005**: 只读守恒 — 收尾时 `git status` 相对开工仅新增 docs/ 两文档与 evolve/ 文件，生产/测试代码零变更。 [FR-005]
- **AC-006**: 证据新鲜 — 报告含「实测指标表」，pytest 收集数 / 相关套件结果 / grep 计数均本轮实测。 [FR-006]
- **AC-007**: 文案合规 — v6 报告 `grep -inE "快速|友好|高效|灵活|强大"` 命中 = 0（术语白名单除外）。 [FR-004]

## 4. 范围

- ✅ **In Scope**: `ea1013b..HEAD` 增量深审；core/gui/inference/scripts 全仓横切；v5 宣称核销；测试体系健康度（收集/通过数、UIA 现状）；v6 报告
- ❌ **Out of Scope**: 不修复任何发现（含 P0——只出清单与波次建议）；不重审 v5 已覆盖的 W26-W34 模块（仅状态复核）；不动 `~/.claude/skills/`、其他技能；不验证已知待办中的「真机 cuda/OCR 离线权重端到端」（需目标硬件，报告只标注）

## 5. 风险与假设（含需求探索三栏）

- **已知**: 仓库开工时 clean（仅 `?? .claude/`）；HEAD=`550ecbf`；v5 止于 W34；13 份 learning 文档含 W29-W35 波次坑位经验（输入）
- **假设（待验证）**: 并行会话当前不在本仓在途（EXP-202608-23d 教训：审查中若 `git status` 出现他方改动 → 暂停复核后再续）→ 否则按该 EXP 处置
- **未知**: 无残留（探索门禁 3/3 已裁决）
- **风险**: ① agent 误报 → 事实核查视角 + 主线 codegraph 双重复核，P0/P1 须两源一致才入清单；② v6 报告膨胀失焦 → 上限控制：只列「有证据的新发现 + 核销结论」，复述架构不重写；③ 审查时间超预期 → 横切深度优先保障增量主审，全仓横切可降为抽样+codegraph 定向

## 6. 实现思路

- **拟采用**: 主线（codegraph 增量调用链深审 + 核销验证）与 5 并行 agent（各自视角扫描+复核）同时开工 → 主线汇总 → 攻方复核（对每条 P0/P1 自我反驳一轮）→ 报告落盘
- **复用**: v5 报告体例与覆盖矩阵模板；codegraph 索引；五视角 agent 编制（用户 agents.md 既定）
- **注意**: agent 只读探查不写码；发现引用一律 `file:line` 并核对当前 HEAD；`705f1ee` 历史折叠异常顺带核实（git log 查两份 SDW 文档的落点 commit，一行结论即可）

---

## 自检（5 项）

- [x] 完整性: FR-001~006 全编号
- [x] 无歧义: `grep -iE "快速|友好|高效|灵活|强大"` 本文件命中 = 0
- [x] 可追溯: 每 FR 有 AC
- [x] 范围清晰: In/Out 已列
- [x] 指标可量化: AC 均有可判定标准

## ✅ 门禁（3 项）

- [x] 门禁 1 探索门禁：AskUserQuestion 3 问已裁决（2026-08-23）
- [x] 门禁 2 PRD：AskUserQuestion 确认 → 进入执行（2026-08-23 用户确认）
- [x] 门禁 3 收尾：报告产出（docs/AutoVisionAgent-架构解析与优化方案-v6.md，P1×2/P2×8/P3×16）+ AC-001~007 全过 + 只读守恒 + 沉淀完成（EXP-2026-08-23g + learning 文档）（2026-08-23 用户确认收尾）
