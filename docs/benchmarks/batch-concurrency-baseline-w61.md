# 批量推理后处理并发基线（W61 · FR-002 / NFR-001 / D-14 收口）

> 日期: 2026-09-02 | 脚本: `benchmarks/bench_batch_concurrency.py`（可复跑） | 机器: 本机 RTX 3060 环境（源码态）
> 口径: 快引擎 + save_overlay=True（负载集中到被并行的后处理层——叠加渲染/产物写）；引擎前向恒串行（W56 设计）。

## 基线数字

| 日期 | n | 串行(concurrency=1) | 并发(concurrency=4) | ratio | 判读 |
|------|---|--------------------|--------------------|-------|------|
| 2026-09-02 12:20 | 50 | 0.36s | 0.34s | **0.96x** | OK（不劣化） |

## 判读与局限（诚实口径）

- **NFR-001 的承诺是「并行不劣化」**：0.96x 满足（≤1.2x 阈值）。
- **局限**：本负载下后处理单图仅 ~7ms（640×480 叠加渲染+写盘），并行收益 < 调度开销，ratio≈1 是预期的中性结果；该基线**证明不劣化，不证明显著加速**。真实重负载（大图/多框/慢渲染）下的加速幅度留待真实数据集场景复测。
- D-14 裁决兑现：不做硬 wall-time 门禁断言（噪声/flaky 风险），实测数字记录于此为健康度基线。

## 排坑留档（同坑二犯，EXP 累计）

首跑挂死在 `_show_stats` 模态 `exec()`（faulthandler 栈：page.py:701←_batch_done）——脚本首版只替换了 `QMessageBox.information` 类方法，而 `_show_stats` 是**函数内局部导入+实例构造**，必须**模块属性级替换** `PySide6.QtWidgets.QMessageBox = _FakeMsgBox`（W56 EXP-20260902c 已记录过的同一条，本批自犯）。
