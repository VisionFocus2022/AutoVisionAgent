# SKolpha SAM 标注机制逆向取证 — 精简 PRD (L2 · 研究波)

> 版本: 1.0 | 日期: 2026-08-24 | 档位: 🟡 L2（研究型：低确定 × 小影响，只读分析外部 exe + 报告产出） | 可逆性: 双向门（纯文档）
> 前置：探索门禁已裁决（静态三层取证 / 报告+映射建议）。目标物：`E:\计算机视觉\最新版-SKolpha3.3.2-更新日期2024.11.18\skolpha.exe`（Nuitka 编译，64.7MB）。

## 1. 背景与目标

- **背景**：AVA 对标复刻 SKolpha，但「标注页矩形框定区域 → 区域内 SAM 分割」的真实实现机制未知（候选：裁剪送 SAM / SAM box prompt / 全图分割后裁剪输出），现有实现靠猜测有行为漂移风险。
- **目标**：以 ≥2 源三角验证给出机制结论 + 对 AVA SamSessionMixin 的实现映射建议。

## 2. 功能需求 (FR)

- **FR-001**: Nuitka 常量字符串挖掘 — 提取 exe 内 SAM/矩形/分割相关标识符（模块名、推理参数名、UI 文案、日志格式串），记录偏移证据。 | P0
- **FR-002**: help.chm 反编译 — `hh.exe -decompile`（或 7z 兜底）提取官方手册中矩形+SAM 标注章节的操作描述。 | P0
- **FR-003**: 配置资产解读 — configFile*.json / TrainConfigs / languageFile；default.yaml 为 Fernet 加密（记录事实即可，不尝试破解）。 | P1
- **FR-004**: 机制结论三角验证 — 三候选（裁剪送 SAM / box prompt / 分割后裁剪）各给支持/反对证据，结论须 ≥2 源。 | P0
- **FR-005**: 报告落盘 — `docs/skolpha-sam-annotation-forensics.md`（结论表+证据链+AVA 映射建议：三机制在 AVA 落地路径与代价对比）。 | P0

## 3. 验收标准 (AC)

- **AC-001**: 字符串证据清单 ≥10 条有效命中（含偏移）或显式记录不足 [FR-001]
- **AC-002**: chm 提取成功且定位到 SAM 相关章节；失败则记录失败模式并靠双源兜底 [FR-002]
- **AC-003**: 机制结论表三候选各有裁决（证实/证伪/无证据），胜出结论 ≥2 源 [FR-004]
- **AC-004**: 报告含 AVA 映射建议（三机制对比 + 推荐路径 + 预估改动面）[FR-005]
- **AC-005**: 全程零运行态触碰（不启动 skolpha.exe）；本仓生产码零改动 [全]

## 4. 范围

- ✅ In: 上述 5 FR；报告 + evolve 沉淀
- ❌ Out: 动态运行 exe；反汇编；default.yaml 破解；与 SAM 标注无关模块；AVA 侧实现（另波）

## 5. 风险与假设

- 已知: Nuitka 保常量/模块名；chm 434MB（提取耗时风险）；AVA SamSessionMixin 为映射目标
- 假设: 手册含该功能描述（boxIcons/ 图标名亦可佐证工具存在）
- 风险: ① Nuitka 常量压缩/混淆致字符串稀疏 → 以 chm+配置双源兜底；② chm 提取巨慢 → 先解目录树再定点抽 HTML

## ✅ 门禁

- [x] 门禁 1 探索门禁（2026-08-24：静态三层 / 报告+映射）
- [x] 门禁 2 PRD：确认 → 执行（2026-08-24 用户确认）
- [x] 门禁 3 收尾：AC-001~005 全过 + 报告落盘 + EXP-2026-08-24c/learning 沉淀（2026-08-24 用户确认收尾）
